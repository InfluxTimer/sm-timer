"Styles"
{
	"nrml" // Use safe name or index. See scripting/include/influx/core.inc for list of indices.
	{
		"name_override"		"Unnatural" // Change the name of the style. Note: it does not currently affect searches (eg. "!wr unnatural" will not work.)
		"order"		"1" // Higher order will raise the style in menus. By default 0.
	}
	"1" // Sideways
	{
		"name_override"		"sIdEwAyZ"
		"shortname_override"		"SideW" // Change the short name of the style. Used by replay menu, record chat print, etc.
		"order"		"1415" // This is the highest order, so it will be the first in menus.
	}
	"parkour"
	{
		"flags"		"z" // Only players will z-flag can use this style.
	}
}