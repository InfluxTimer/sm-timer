"Modes"
{
	"scrl" // Use safe name or index. See scripting/include/influx/core.inc for list of indices.
	{
		"name_override"		"Legit" // Change the name of the mode. Note: it does not currently affect searches (eg. "!wr legit" will not work.)
		"order"		"1" // Higher order will raise the mode in menus. By default 0.
	}
	"1" // Autobhop
	{
		"name_override"		"auto is dumb"
		"shortname_override"		"Autob" // Change the short name of the mode. Used by replay menu, record chat print, etc.
		"order"		"1337" // This is the highest order, so it will be the first in menus.
	}
	"velcap"
	{
		"name_override"		"400vel"
		"shortname_override"		"400vel"
		"flags"		"z" // Only players will z-flag can use this mode.
	}
}